[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "waxs-cellulose"
version = "0.1.0"
description = "WAXS analysis tools for cellulosic materials"
authors = [{name = "Patrik Ahvenainen"}]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "pandas",
    "lmfit",
]

[tool.pytest.ini_options]
testpaths = ["tests"]